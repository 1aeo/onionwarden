# roles/generic.conf — default role profile (PLAN §0.6).
# Loaded BEFORE host.conf; every value here is overridable per host.
role_label               = "generic"
workload_integrity_check = "none"
outbound_mode            = "allowlist"
